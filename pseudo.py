# create select all function for tag repository

# declare empty list to store tags

# assign sql query to sql variable (i.e. select all from tags)

# declare rows and call run_sql, passing in sql variable above

# run for loop on rows. Create an instance of tag for each row returned by sql, using 'category' and 'id'

# append each instance of tag to the list of tags

# return full list3